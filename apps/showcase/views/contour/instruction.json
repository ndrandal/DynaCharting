{
  "id": "static-contour",
  "note": "Static texture view (walled tier) — NO embassy capture. The scalar field (heatmap.json) is CPU-processed via marching-squares into filled iso-bands + isolines and rasterized to an RGBA8 colormap at build time (see manifest.ts textures[]); records.json is an empty stub. This file is a placeholder for the per-view directory convention.",
  "sessionId": "showcase",
  "subscriptions": [],
  "visualization": { "recipe": "static-texture-v1", "bindings": [], "buffers": [] }
}
